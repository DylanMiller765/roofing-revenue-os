# Future Supabase persistence (not used in mock mode)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Future Google Ads reporting adapter (not connected in mock mode)
GOOGLE_ADS_DEVELOPER_TOKEN=
GOOGLE_ADS_CLIENT_ID=
GOOGLE_ADS_CLIENT_SECRET=
GOOGLE_ADS_REFRESH_TOKEN=
GOOGLE_ADS_LOGIN_CUSTOMER_ID=
GOOGLE_ADS_CUSTOMER_ID=

# Controlled mutation adapter stays disconnected unless explicitly enabled in a
# separate deployment. This flag alone never bypasses risk or safety policy.
GOOGLE_ADS_MUTATIONS_ENABLED=false
